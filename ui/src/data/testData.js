// Mocks for call() in dev (NUI -> Lua with a response).
// The radial menu does not use call() for now (push + select only).
// Add a mock per callback here if any are introduced (see ui/docs/NUI_CONTRACT.md).
export const testResponses = {};
